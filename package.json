{
  "name": "risk-v",
  "version": "0.1.0",
  "description": "A RISK-V emulator in Typescript",
  "main": "dist/index.js",
  "repository": "https://github.com/salty-max/risk-v.git",
  "author": "Maxime Blanc <max@jellycat.fr>",
  "license": "MIT",
  "scripts": {
    "build": "rimraf dist && tsc",
    "watch": "tsc -w",
    "dev": "ts-node src/index.ts",
    "lint": "rome check src",
    "fmt": "rome format --write src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^20.4.5",
    "rimraf": "^5.0.1",
    "rome": "^12.1.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  }
}